import styled from "styled-components";

export const Container = styled.div`
  display: flex;
  padding: 1rem;

  a {
    margin: 0 2rem;
    font-size: 1.3rem;
    color: var(--grey-100);
    cursor: pointer;
  }
`;
